# Configuración de la base de datos (local)
spring.datasource.url=jdbc:mysql://sql10.freesqldatabase.com:3306/sql10756847
spring.datasource.username=sql10756847
spring.datasource.password=xWv2FQ1f9Z
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Logging
logging.level.ar.com.envios=DEBUG
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=trace

# Inicialización de la base de datos
spring.datasource.initialize=true

# JWT Configuración
jwt.issuer=facturacion
jwt.secret=clave_segura

# Configuración del servidor
server.address=0.0.0.0
server.port=8080

spring.main.allow-bean-definition-overriding=true
